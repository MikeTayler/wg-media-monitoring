/**
 * TODO: Hardcoded PoC configuration (entities, aliases, keywords, recipients).
 * - Replace placeholder keywords with confirmed participant input (see `project.md`).
 * - Include a "global" entity for sector-wide topics all recipients should see.
 * - No admin UI — edit this file and redeploy for PoC changes.
 */

import type { Entity } from "@/lib/types";

// TODO: Populate with real entities and email addresses.
export const entities: Entity[] = [];
