/**
 * TODO: Send email via Mailgun (`mailgun.js`).
 * - Use `MAILGUN_API_KEY`, `MAILGUN_DOMAIN`, `MAILGUN_FROM` from environment.
 * - Called by `/api/digest` for each recipient (or batched if you add BCC later — project says one email per recipient).
 * - Return success/failure for logging; do not throw in a way that stops other sends unnecessarily.
 */

export {};
