/**
 * TODO: HTML email template for the daily digest.
 * - Inline CSS only; mobile-friendly layout.
 * - Group by entity; sort articles by relevance score within each section.
 * - Show headline (linked), source name, High/Medium relevance indicator, AI summary.
 * - NZ Herald: show a “Paywalled” badge.
 * - Empty day: short “No relevant coverage found today” message.
 * - Accept structured data (e.g. per-recipient matches) from the digest route.
 */

export {};
