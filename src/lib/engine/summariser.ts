/**
 * TODO: Article summaries via OpenRouter → Claude Haiku.
 * - Same API key and model as `ai-scorer.ts`.
 * - 1–2 factual, neutral sentences; no editorialising.
 * - For NZ Herald (`paywalled: true`), summarise from title + RSS description only.
 * - try/catch around calls; on failure, log and use a short fallback or empty summary as decided during implementation.
 */

export {};
